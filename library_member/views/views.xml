<odoo>
 

    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="library_member.list">
      <field name="name">library_member list</field>
      <field name="model">library_member.library_member</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="library_member.action_window">
      <field name="name">library_member window</field>
      <field name="res_model">library_member.library_member</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="library_member.action_server">
      <field name="name">library_member server</field>
      <field name="model_id" ref="model_library_member_library_member"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->
    <record id= "library_member.action_window" model= "ir.actions.act_window">
      <field name="name">Members</field>
      <field name="res_model">bibliotheque.book</field>
      <field name="view_mode">tree,form</field>
    </record>

    <!-- Top menu item -->
    
   <menuitem name="Library_member" id="library_member.menu_root"/>
    
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="library_member.menu_1" parent="library_member.menu_root"/>
    <menuitem name="Menu 2" id="library_member.menu_2" parent="library_member.menu_root"/>
    -->
    <!-- actions -->
    
   <menuitem name="Members" id="library_member_list" parent="library_member.menu_root"
              action="library_member.action_window"/>
    <!--<menuitem name="Server to list" id="library_member" parent="library_member.menu_2"
              action="library_member.action_server"/>
    -->
  
</odoo>